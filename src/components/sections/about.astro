---
const locate = Astro.currentLocale;
let module = locate === "es" ? await import("@data") : await import("@data.en");
const data = module.default;

const { about } = data.basics;
---

<section>
  <h2>{ locate === "es" ? "Sobre Mi" : "About"}</h2>
  <p>
    {about}
  </p>
</section>

<style>
  p {
    color: #818589;
  }
</style>
